# AGENTS.md — {{name}}

## Purpose
This project was forged by the **Foundry Software Factory**. It follows strict industrial standards for code quality and fleet integration.

## Context
- **Project Name**: {{name}}
- **Standard**: Foundry Gold Standard (v1)
- **Infrastructure**: Managed via `fnd` (Foundry Commander)
- **Backend services**: Use `@saas-maker/sdk` when linking to the fleet API.

## Standard Procedures for Agents

### 1. Code Changes
- Always use **Standardized Imports**: Built-ins first, then external packages, then local modules.
- Prefer `@saas-maker/sdk` for feedback, waitlist, changelog, and other fleet API calls.

### 2. Failure Handling
- Wrap critical boundaries in try/catch with clear error messages.
- Check the **Foundry Cockpit** error feed if reproduction is difficult.

### 3. Maintenance
- Run `fnd audit` inside this directory to verify compliance.
- Run `fnd fleet upgrade` to refresh lint/format devDependencies.

## Reference Skills
- See `saas-maker/skills/protocol-debugging.md` for investigation steps.
- See `saas-maker/skills/protocol-migration.md` for refactoring legacy code.
