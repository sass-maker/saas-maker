# Project Docs

Follow the fleet documentation boundary in `../../README.md`.

Agent-facing rules live in `../../AGENTS.md`: create Symphony tasks by default;
use docs only for durable reference, design, research, and runbooks.

For Fleet-level operations, do not duplicate guidance here. In the local Fleet
workspace, use:

- `../../docs/fleet-runbook.md` for how to run, verify, deploy, push, and clean
  Fleet projects.
- `../../docs/project-map.md` for how SaaS Maker, Symphony, GitHub, local
  agents, and deployment targets connect.
- `docs/symphony.md` for SaaS Maker-specific Symphony command details.
- `docs/droid.md` for Droid sandbox runner quickstart and API fields.

On GitHub, the shared Fleet docs live in
`sarthakagrawal927/fleet-workspace`.
