# SaaS Maker Knowledge System

This `docs/` tree is the **canonical, local-first knowledge system** for the
SaaS Maker repository. Markdown committed here is the source of truth for
product knowledge, architecture, decisions, development workflows, operations,
and durable learnings. Code and executable configuration remain authoritative
for implementation details and schedules.

[Blume](https://github.com/sass-maker/saas-maker/tree/main/apps/docs-blume) (`apps/docs-blume/`)
is the only production presentation and search layer that renders this tree,
served under `sassmaker.com/docs`. It does not own content. The legacy
Astro/Starlight site at `apps/docs/` is archived and must not be deployed. When
copies diverge, the `docs/` tree here wins.

## Layout

| Path | Purpose |
| --- | --- |
| [`index.mdx`](index.mdx) | Public landing page (rendered by Blume). |
| [`getting-started/`](getting-started/introduction.md) | First-touch guides: intro, quickstart, auth, integration, the Standard. |
| [`api/`](api/overview.md) | REST API reference (OpenAPI-derived). |
| [`sdk/`](sdk/cli.md) | `fnd` CLI and TypeScript SDK usage. |
| [`services/`](services/projects.md) | Product services: projects, tasks, feedback, changelog, waitlist, testimonials, roadmap. |
| [`widgets/`](widgets/feedback.md) | Embeddable widget recipes. |
| [`current/`](current/README.md) | Current objective, active work, and status pointers. |
| [`product/`](product/README.md) | Product knowledge: what SaaS Maker is, fleet registry, ideas, recommendation context. |
| [`architecture/`](architecture/README.md) | System architecture: Symphony, Droid, task-cloud mapping, plus `decisions/` (design records) and `research/`. |
| [`architecture/decisions/`](architecture/decisions/README.md) | Durable design decisions and dated plans (ADR-style). |
| [`architecture/research/`](architecture/research/README.md) | Research notes that informed decisions. |
| [`development/`](development/README.md) | Development workflows: testing backlog, quality gates. |
| [`operations/`](operations/README.md) | Operations: Cloudflare secrets, shields, baselines, migrations, PostHog, automation setup, launch kit. |
| [`operations/jobs/`](operations/jobs/README.md) | Scheduled jobs (codex-automations cron catalog). |
| [`operations/runbooks/`](operations/runbooks/README.md) | Step-by-step runbooks for fleet audits, smoke, and recovery. |
| [`knowledge/`](knowledge/README.md) | Durable learnings and failed approaches worth remembering. |
| [`knowledge/learnings/`](knowledge/learnings/README.md) | Novel primitives and patterns powering the stack. |
| [`knowledge/failed-approaches/`](knowledge/failed-approaches/README.md) | Removed/shelved approaches and why they did not ship. |
| [`org-profiles/`](org-profiles/README.md) | Per-organization fleet profile READMEs. |
| [`openapi/`](openapi/README.md) | Generated OpenAPI artifact (regenerated by `pnpm generate:openapi`). |
| [`superpowers/`](superpowers/README.md) | Linkchat-specific design notes parked here (cross-fleet artifact). |

## Root-level docs

These live at the repository root (outside the `docs/` tree), so the links
below point to GitHub:

| File | Role |
| --- | --- |
| [`AGENTS.md`](https://github.com/sass-maker/saas-maker/blob/main/AGENTS.md) | Concise agent bootloader — purpose, commands, constraints, doc navigation. |
| [`STATUS.md`](https://github.com/sass-maker/saas-maker/blob/main/STATUS.md) | Short current-state view: objective, active work, blockers, next steps. |
| [`PROJECT_STATUS.md`](https://github.com/sass-maker/saas-maker/blob/main/PROJECT_STATUS.md) | Detailed timeline + shipped features. Kept at root because `pnpm check:fleet-contracts` verifies its presence across the fleet. |
| [`WORKFLOW.md`](https://github.com/sass-maker/saas-maker/blob/main/WORKFLOW.md) | Symphony execution policy (frontmatter config + prose). |
| [`AUDIT.md`](https://github.com/sass-maker/saas-maker/blob/main/AUDIT.md) | Security and quality audit log. |
| [`README.md`](https://github.com/sass-maker/saas-maker/blob/main/README.md) | Repo overview, fleet registry, quick start. |
| [`CONTRIBUTING.md`](https://github.com/sass-maker/saas-maker/blob/main/CONTRIBUTING.md) / [`SECURITY.md`](https://github.com/sass-maker/saas-maker/blob/main/SECURITY.md) | Contribution and security policy. |

## Maintenance rules

1. **One home per fact.** Do not duplicate a concept in two files. Link from
   secondary surfaces to the canonical home.
2. **Markdown is the source of truth.** Blume is the production presentation
   layer; never edit content inside `apps/docs-blume/dist/` or the archived
   `apps/docs/` tree.
3. **Let Blume own the page title.** Give each page a `title` in frontmatter
   and start the body at `##`; a leading `#` repeats the title in the rendered
   page.
4. **Record why, not what.** Code shows what. Document non-obvious constraints,
   operational procedures, important decisions, and reusable failed approaches.
5. **Mark unresolved questions explicitly** (e.g., "TBD", "Open question:").
6. **Prefer preservation over deletion** — superseded plans live under
   `architecture/decisions/` with their original dates so git rename history
   stays intact. Removed/shelved approaches live under
   `knowledge/failed-approaches/`.
7. **When API routes change**, run `pnpm generate:openapi` (updates
   `docs/openapi/` and `packages/cli/src/openapi.json`)
   and update the relevant `services/` or `sdk/` page.
8. **Validate before pushing**: `node scripts/check-docs.mjs` checks broken
   links, empty docs, and required files. It runs in CI.
9. **Keep pages focused** (150–300 lines). Split catch-all docs into per-topic
   pages and link between them.

## Adding documentation

- New design decision → `architecture/decisions/<date>-<topic>.md`.
- New runbook → `operations/runbooks/<name>.md`.
- New scheduled job → add a row to `operations/jobs/README.md`.
- New learning → `knowledge/learnings/<topic>.md`.
- Removed/shelved feature → `knowledge/failed-approaches/<topic>.md` with the
  reason and the commit that removed it.
- Public product doc → `getting-started/`, `api/`, `sdk/`, `services/`, or
  `widgets/` (these are rendered by Blume).
