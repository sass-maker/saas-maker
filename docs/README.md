# SaaS Maker Knowledge System

This `docs/` tree is the **canonical, local-first knowledge system** for the
SaaS Maker repository. Markdown committed here is the source of truth for
product knowledge, architecture, decisions, development workflows, operations,
and durable learnings. Code and executable configuration remain authoritative
for implementation details and schedules.

[Blume](../apps/docs-blume/) (`apps/docs-blume/`) is the presentation and search
layer that renders this tree for `docs.sassmaker.com`. It does not own content.
The legacy Astro/Starlight site at `apps/docs/` keeps its own copy of the public
product docs until the Blume cutover is complete — when those diverge, the
`docs/` tree here wins.

## Layout

| Path | Purpose |
| --- | --- |
| [`index.mdx`](index.mdx) | Public landing page (rendered by Blume). |
| [`getting-started/`](getting-started/) | First-touch guides: intro, quickstart, auth, integration, the Standard. |
| [`api/`](api/) | REST API reference (OpenAPI-derived). |
| [`sdk/`](sdk/) | `fnd` CLI and TypeScript SDK usage. |
| [`services/`](services/) | Product services: projects, tasks, feedback, changelog, waitlist, testimonials, roadmap. |
| [`widgets/`](widgets/) | Embeddable widget recipes. |
| [`current/`](current/) | Current objective, active work, and status pointers. |
| [`product/`](product/) | Product knowledge: what SaaS Maker is, fleet registry, ideas, recommendation context. |
| [`architecture/`](architecture/) | System architecture: Symphony, Droid, task-cloud mapping, plus `decisions/` (design records) and `research/`. |
| [`architecture/decisions/`](architecture/decisions/) | Durable design decisions and dated plans (ADR-style). |
| [`architecture/research/`](architecture/research/) | Research notes that informed decisions. |
| [`development/`](development/) | Development workflows: testing backlog, quality gates. |
| [`operations/`](operations/) | Operations: Cloudflare secrets, shields, baselines, migrations, PostHog, automation setup, launch kit. |
| [`operations/jobs/`](operations/jobs/) | Scheduled jobs (codex-automations cron catalog). |
| [`operations/runbooks/`](operations/runbooks/) | Step-by-step runbooks for fleet audits, smoke, and recovery. |
| [`knowledge/`](knowledge/) | Durable learnings and failed approaches worth remembering. |
| [`knowledge/learnings/`](knowledge/learnings/) | Novel primitives and patterns powering the stack. |
| [`knowledge/failed-approaches/`](knowledge/failed-approaches/) | Removed/shelved approaches and why they did not ship. |
| [`org-profiles/`](org-profiles/) | Per-organization fleet profile READMEs. |
| [`openapi/`](openapi/) | Generated OpenAPI artifact (regenerated by `pnpm generate:openapi`). |
| [`superpowers/`](superpowers/) | Linkchat-specific design notes parked here (cross-fleet artifact). |

## Root-level docs

| File | Role |
| --- | --- |
| [`AGENTS.md`](../AGENTS.md) | Concise agent bootloader — purpose, commands, constraints, doc navigation. |
| [`STATUS.md`](../STATUS.md) | Short current-state view: objective, active work, blockers, next steps. |
| [`PROJECT_STATUS.md`](../PROJECT_STATUS.md) | Detailed timeline + shipped features. Kept at root because `pnpm check:fleet-contracts` verifies its presence across the fleet. |
| [`WORKFLOW.md`](../WORKFLOW.md) | Symphony execution policy (frontmatter config + prose). |
| [`AUDIT.md`](../AUDIT.md) | Security and quality audit log. |
| [`README.md`](../README.md) | Repo overview, fleet registry, quick start. |
| [`CONTRIBUTING.md`](../CONTRIBUTING.md) / [`SECURITY.md`](../SECURITY.md) | Contribution and security policy. |

## Maintenance rules

1. **One home per fact.** Do not duplicate a concept in two files. Link from
   secondary surfaces to the canonical home.
2. **Markdown is the source of truth.** Blume and the Astro site are
   presentation layers; never edit content inside `apps/docs-blume/dist/` or
   `apps/docs/dist/`.
3. **Record why, not what.** Code shows what. Document non-obvious constraints,
   operational procedures, important decisions, and reusable failed approaches.
4. **Mark unresolved questions explicitly** (e.g., "TBD", "Open question:").
5. **Prefer preservation over deletion** — superseded plans live under
   `architecture/decisions/` with their original dates so git rename history
   stays intact. Removed/shelved approaches live under
   `knowledge/failed-approaches/`.
6. **When API routes change**, run `pnpm generate:openapi` (updates
   `docs/openapi/`, `packages/cli/src/openapi.json`, `apps/docs/public/openapi.json`)
   and update the relevant `services/` or `sdk/` page.
7. **Validate before pushing**: `node scripts/check-docs.mjs` checks broken
   links, empty docs, and required files. It runs in CI.
8. **Keep pages focused** (150–300 lines). Split catch-all docs into per-topic
   pages and link between them.

## Adding documentation

- New design decision → `architecture/decisions/<date>-<topic>.md`.
- New runbook → `operations/runbooks/<name>.md`.
- New scheduled job → add a row to `operations/jobs/README.md`.
- New learning → `knowledge/learnings/<topic>.md`.
- Removed/shelved feature → `knowledge/failed-approaches/<topic>.md` with the
  reason and the commit that removed it.
- Public product doc → `getting-started/`, `api/`, `sdk/`, `services/`, or
  `widgets/` (these are rendered by Blume).
