---
import Footer from '../components/Footer.astro';
import Nav from '../components/Nav.astro';
import { SEARCH_INDEX, SEARCH_TYPES, type SearchEntry } from '../data/searchIndex';
import Layout from '../layouts/Layout.astro';

const TYPE_LABELS: Record<string, string> = {
  page: 'Pages',
  product: 'Products',
  idea: 'Ideas',
  funding: 'Funding',
  doc: 'Docs',
  launchdesk: 'Launch catalog',
  tool: 'Tools',
  learning: 'Learnings',
};

const groups = SEARCH_TYPES.map((type) => ({
  type,
  label: TYPE_LABELS[type] ?? type,
  entries: SEARCH_INDEX.filter((entry) => entry.type === type),
}));

// Emit only keyword tokens that aren't already visible in the row — the rest
// would be pure duplication since the script matches against textContent+href.
const keywords = (entry: SearchEntry) => {
  const visible = `${entry.title} ${entry.summary} ${entry.url}`.toLowerCase();
  const delta = entry.keywords
    .toLowerCase()
    .split(/\s+/)
    .filter((token) => token && !visible.includes(token));
  return delta.length ? delta.join(' ') : undefined;
};
---

<Layout
  title="Search — SaaS Maker"
  description="Search every public SaaS Maker surface — products, ideas, funding programs, launch destinations, tools, and learnings."
>
  <div class="workshop-world interior-world tooling-world">
    <Nav />
    <main id="main-content" class="search-shell">
      <header class="tooling-intro">
        <div class="tooling-intro-copy">
          <p class="directory-kicker">Everything, one index</p>
          <h1>Search the whole site.</h1>
          <p>
            {SEARCH_INDEX.length} entries across products, ideas, funding programs, launch
            destinations, tools, docs, and learnings. Agents: the same index lives at
            <a href="/search.json">/search.json</a> — fetch once, filter on
            <code>title + summary + keywords</code>.
          </p>
        </div>
        <div class="tooling-count" aria-label={`${SEARCH_INDEX.length} indexed entries`}>
          <strong>{SEARCH_INDEX.length}</strong>
          <span>entries · {SEARCH_TYPES.length} types</span>
        </div>
      </header>

      <div class="search-controls">
        <input
          id="search-input"
          type="search"
          placeholder="Search products, programs, domains, tools…"
          autocomplete="off"
          aria-label="Search the site"
        />
        <div class="search-types" id="search-types" role="group" aria-label="Filter by type">
          {groups.map((group) => (
            <button type="button" data-type={group.type} class="type-chip">
              {group.label} <span>{group.entries.length}</span>
            </button>
          ))}
        </div>
      </div>

      <p id="search-count" class="search-count" role="status" aria-live="polite">
        {SEARCH_INDEX.length} entries — filter above or browse below.
      </p>

      {groups.map((group) => (
        <section class="search-group" data-group={group.type}>
          <h2 class="group-head">{group.label} <span>{group.entries.length}</span></h2>
          <ol class="search-results">
            {group.entries.map((entry) => (
              <li data-k={keywords(entry)}>
                <a href={entry.url}>
                  <span class="result-title">{entry.title}</span>
                  {entry.summary && <span class="result-summary">{entry.summary}</span>}
                </a>
              </li>
            ))}
          </ol>
        </section>
      ))}
    </main>
    <Footer />
  </div>
</Layout>

<style is:global>
  .search-shell {
    width: min(86rem, calc(100% - 3rem));
    margin-inline: auto;
    padding: clamp(2rem, 4vw, 3rem) 0 clamp(5rem, 9vw, 8rem);
  }

  .search-shell .search-controls {
    margin-top: 1.75rem;
    display: flex;
    flex-direction: column;
    gap: 0.75rem;
    position: sticky;
    top: 0;
    z-index: 10;
    padding-block: 0.75rem;
    background: var(--bg);
  }

  .search-shell #search-input {
    width: 100%;
    max-width: 42rem;
    padding: 0.7rem 1rem;
    font: inherit;
    font-size: 1rem;
    color: var(--ink);
    background: var(--surface);
    border: 1px solid var(--line-strong);
    border-radius: 10px;
  }

  .search-shell #search-input:focus-visible {
    outline: 2px solid var(--copper-hi);
    outline-offset: 2px;
  }

  .search-shell .search-types {
    display: flex;
    flex-wrap: wrap;
    gap: 0.4rem;
  }

  .search-shell .type-chip {
    padding: 0.3rem 0.75rem;
    font: inherit;
    font-size: 0.78rem;
    letter-spacing: 0.04em;
    text-transform: uppercase;
    color: var(--ink-soft);
    background: var(--surface);
    border: 1px solid var(--line);
    border-radius: 999px;
    cursor: pointer;
  }

  .search-shell .type-chip span {
    color: var(--ink-muted);
    margin-left: 0.25rem;
  }

  .search-shell .type-chip[aria-pressed='true'] {
    color: var(--surface);
    background: var(--ink);
    border-color: var(--ink);
  }

  .search-shell .search-count {
    margin: 1.25rem 0 0;
    font-size: 0.82rem;
    color: var(--ink-muted);
  }

  .search-shell .search-group {
    margin-top: 2rem;
  }

  .search-shell .search-group[hidden] {
    display: none;
  }

  .search-shell .group-head {
    font-size: 0.85rem;
    letter-spacing: 0.08em;
    text-transform: uppercase;
    color: var(--ink-muted);
    margin-bottom: 0.5rem;
  }

  .search-shell .group-head span {
    color: var(--ink-soft);
    font-weight: 400;
  }

  .search-shell .search-results {
    margin: 0;
    padding: 0;
    list-style: none;
    border: 1px solid var(--line);
    border-radius: 14px;
    background: var(--surface);
    overflow: hidden;
  }

  .search-shell .search-results li[hidden] {
    display: none;
  }

  .search-shell .search-results li + li {
    border-top: 1px solid var(--line);
  }

  .search-shell .search-results a {
    display: flex;
    flex-wrap: wrap;
    column-gap: 1.25rem;
    row-gap: 0.25rem;
    padding: 0.6rem 1.1rem;
    color: var(--ink);
    text-decoration: none;
  }

  .search-shell .search-results a:hover {
    background: color-mix(in srgb, var(--ink) 4%, var(--surface));
  }

  .search-shell .result-title {
    font-weight: 600;
    flex: 0 1 22rem;
    min-width: 0;
    overflow: hidden;
    text-overflow: ellipsis;
    white-space: nowrap;
    order: 1;
  }

  .search-shell .search-results a::after {
    content: attr(href);
    order: 2;
    font-size: 0.78rem;
    color: var(--ink-muted);
    flex: 1 1 12rem;
    min-width: 0;
    overflow: hidden;
    text-overflow: ellipsis;
    white-space: nowrap;
    align-self: center;
  }

  .search-shell .result-summary {
    order: 3;
    flex: 0 0 100%;
    font-size: 0.82rem;
    color: var(--ink-soft);
    display: -webkit-box;
    -webkit-line-clamp: 2;
    -webkit-box-orient: vertical;
    overflow: hidden;
  }
</style>

<script>
  const input = document.getElementById('search-input') as HTMLInputElement;
  const count = document.getElementById('search-count') as HTMLElement;
  const chips = [...document.querySelectorAll<HTMLButtonElement>('.type-chip')];
  const groups = [...document.querySelectorAll<HTMLElement>('.search-group')];
  const rows = [...document.querySelectorAll<HTMLLIElement>('.search-results li')];
  const activeTypes = new Set<string>();

  const params = new URLSearchParams(location.search);
  const q = params.get('q');
  const t = params.get('type');
  if (q) input.value = q;
  if (t && chips.some((c) => c.dataset.type === t)) {
    activeTypes.add(t);
    chips.find((c) => c.dataset.type === t)!.setAttribute('aria-pressed', 'true');
  }

  // Cache each row's haystack once: visible text (title + url + summary) plus
  // the invisible keywords attribute — nothing is duplicated in the markup.
  for (const row of rows) {
    row.dataset.hay =
      `${row.textContent} ${row.querySelector('a')!.getAttribute('href')} ${row.dataset.k ?? ''}`.toLowerCase();
    row.removeAttribute('data-k');
  }

  function run() {
    const terms = input.value.trim().toLowerCase().split(/\s+/).filter(Boolean);
    let visible = 0;
    for (const row of rows) {
      const groupType = row.closest('.search-group')!.getAttribute('data-group')!;
      const show =
        (!activeTypes.size || activeTypes.has(groupType)) &&
        (!terms.length || terms.every((term) => row.dataset.hay!.includes(term)));
      row.hidden = !show;
      if (show) visible++;
    }
    for (const group of groups) {
      const type = group.getAttribute('data-group')!;
      const typeAllowed = !activeTypes.size || activeTypes.has(type);
      const hasVisible = !!group.querySelector('li:not([hidden])');
      group.hidden = !typeAllowed || !hasVisible;
    }
    count.textContent = terms.length
      ? `${visible} result${visible === 1 ? '' : 's'}`
      : `${visible} entries — filter above or browse below.`;
  }

  input.addEventListener('input', run);
  chips.forEach((chip) =>
    chip.addEventListener('click', () => {
      const type = chip.dataset.type!;
      if (activeTypes.delete(type)) chip.removeAttribute('aria-pressed');
      else {
        activeTypes.add(type);
        chip.setAttribute('aria-pressed', 'true');
      }
      run();
    })
  );
  run();
</script>
