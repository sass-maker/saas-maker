---
// Foundry landing — composition mirrors the original Next.js page.js:
// BackgroundFX → Nav → Hero → Ticker → Fleet (§01) → Spec (§02) → CTA → Footer.
// Each section keeps the same class names so the ported globals.css
// applies unchanged.
---

<Layout>
  <BackgroundFX />
  <Nav />
  <Hero />
  <Ticker />
  <Fleet />
  <Spec />
  <CTA />
  <Footer />
</Layout>
